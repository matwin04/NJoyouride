info:
  name: bsa
  type: http
  seq: 4

http:
  method: GET
  url: "{{bartLegacyBaseUrl}}/bsa.aspx/?cmd=count&key={{apiKey}}&json=Y"
  params:
    - name: cmd
      value: count
      type: query
    - name: key
      value: "{{apiKey}}"
      type: query
    - name: json
      value: Y
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
