info:
  name: ETD copy
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8088/api/station-info/bikeshare?stationId=DALY
  params:
    - name: stationId
      value: DALY
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
