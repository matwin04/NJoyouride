info:
  name: Directions
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8088/api/directions?orig=SFIA&dest=DALY
  params:
    - name: orig
      value: SFIA
      type: query
    - name: dest
      value: DALY
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
